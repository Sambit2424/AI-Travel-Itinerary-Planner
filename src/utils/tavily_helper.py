# This module is extract structured info from tavily for ground data

# Tavily -- top 5 results - 1000 tokens each is too verbose - we need to make it succinct
# eval - ground data should be to the point